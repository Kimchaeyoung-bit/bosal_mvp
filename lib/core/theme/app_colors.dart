import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  static const bg = Color(0xFFF7F5FF);
  static const surface = Color(0xFFFFFFFF);
  static const text = Color(0xFF1A0A3E);
  static const textSub = Color(0xFF9090AA);
  static const border = Color(0xFFEAE6F5);

  // Primary: deep purple
  static const primary = Color(0xFF3D2785);
  static const primaryDark = Color(0xFF2D1569);
  static const primarySoft = Color(0xFFEEE8FF);

  // Accent: pink
  static const accent = Color(0xFFD060B0);
  static const accentSoft = Color(0xFFFCEEF6);
  static const accentDark = Color(0xFFAA3A88);

  // Header background gradient
  static const headerBgStart = Color(0xFFEEE8FF);
  static const headerBgEnd = Color(0xFFFDF5FF);

  // Rank badge colors
  static const rankOne = Color(0xFF3D2785);
  static const rankTwo = Color(0xFFD060B0);
  static const rankThree = Color(0xFFB5B2CC);

  // Banner gradient
  static const bannerStart = Color(0xFF1E0A5C);
  static const bannerEnd = Color(0xFF7B3FAA);

  // CTA salmon/pink
  static const ctaPink = Color(0xFFE87878);

  static const danger = Color(0xFFC0392B);
  static const white = Color(0xFFFFFFFF);
  static const black = Color(0xFF000000);
}
