import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  static const bg = Color(0xFFF4F4F2);
  static const surface = Color(0xFFFFFFFF);
  static const text = Color(0xFF0D0D0D);
  static const textSub = Color(0xFF888888);
  static const border = Color(0xFFE8E8E8);

  // Primary: dark charcoal (replaces purple)
  static const primary = Color(0xFF141414);
  static const primaryDark = Color(0xFF000000);
  static const primarySoft = Color(0xFFF0F0EE);

  // Accent: lime green (key highlight)
  static const accent = Color(0xFFC6EF2A);
  static const accentSoft = Color(0xFFF2FAD4);
  static const accentDark = Color(0xFF8FB800);

  static const danger = Color(0xFFC0392B);
  static const white = Color(0xFFFFFFFF);
  static const black = Color(0xFF000000);
}
